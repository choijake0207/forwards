import React from 'react'
import styles from "../../styles/progressCard.module.css"
export default function ProgressGrid({windowType, habit, dayCount}) {
    console.log(dayCount)
  return (
    <div className={styles.progress_grid}>
      
    </div>
  )
}
